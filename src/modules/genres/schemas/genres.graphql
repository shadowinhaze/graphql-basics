type Genre {
  _id: ID!
  name: String
  description: String
  country: String
  year: Int
}

type AllGenres {
  items: [Genre]
  total: Int
}

type Query {
  genres(limit: Int, offset: Int): AllGenres
  genre(id: ID!): Genre
}
