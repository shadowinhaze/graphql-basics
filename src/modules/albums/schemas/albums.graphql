type Album {
  id: ID!
  name: String
  released: Int
  artists: [Artist]
  bands: [Band]
  tracks: [Track]
  genres: [Genre]
  image: String
}

type AllAlbums {
  items: [Album]
  total: Int
}

type Query {
  albums(limit: Int, offset: Int): AllAlbums
  album(id: ID!): Album
}
